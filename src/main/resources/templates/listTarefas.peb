{% extends "templates/base.peb" %}

{% block content %}

{% if resultado.foiErro %}
<h1> {{ resultado.getMsg }}</h1>
{% else %}
    <ul>
    {% for tarefa in lista %}
        <li>{{tarefa.titulo}} ({{tarefa.categoria.titulo}})</li>
    {% endfor %}
    </ul>
{% endif %}


{% endblock %}